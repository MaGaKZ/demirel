<%= form_for([@subject, @assignment],:html=>{:multipart=>true}) do |f| %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <%= f.file_field :attachment%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
