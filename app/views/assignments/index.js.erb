$( document ).ready(function() {
  
  $('.renders' ).html( ' <%=j render ("index")%> ' );
    $('.list-group-item').click(function(){ 
      $('.list-group-item').removeClass("active");
      $(this).addClass("active");
    });
});
